const {
  getUsers,
  getUser,
  postUser,
  deleteUser,
  putUser,
} = require('../users');

describe('getUsers', () => {
  it('should get users collection', (done) => {
    done();
  });
});
describe('getUsers', () => {
  it('should get users collection', (done) => {
    done();
  });
});

describe('getUsers', () => {
  it('should get users collection', (done) => {
    done();
  });
});

describe('getUser', () => {
  it('should get users collection', (done) => {
    done();
  });
});

describe('deleteUsers', () => {
  it('should delete user collection', (done) => {
    done();
  });
});

describe('postUsers', () => {
  it('should get users collection', (done) => {
    done();
  });
});

describe('putUsers', () => {
  it('should get users collection', (done) => {
    done();
  });
});
